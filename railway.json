{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd note_app && python -m pip install --upgrade pip && pip install -r ../requirements.txt",
    "rootDirectory": "."
  },
  "deploy": {
    "startCommand": "cd note_app && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn note_app.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}